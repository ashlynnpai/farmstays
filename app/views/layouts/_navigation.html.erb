<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
  <ul class="nav navbar-nav">
    <li>
      <div class='nav_item navbar-brand'>
        <%= link_to 'Farmstays', root_path%>
       </div>
     </li>
  <form class="navbar-form navbar-left" role="search">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search for farms">
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
      </form>

  <div class='btn-group nav_item navbar-right'>
      <%= link_to '#', class: 'dropdown-toggle', 'data-toggle' => 'dropdown' do %>
    <h4>Choose a Region <span class='caret'></span></h4>
      <% end %>
        <ul class='dropdown-menu'>
          <% Region.all.each do |region| %>
            <li>
              <%= link_to region.name, region_path(region)%>
            </li>
          <% end %>
        </ul>
    </div>

  </ul>
</div>