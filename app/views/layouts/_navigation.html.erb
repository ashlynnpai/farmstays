<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
  <ul class="nav navbar-nav">
    <li>
      <div class='nav_item'>
        <%= link_to 'Home', root_path, class: 'btn btn-warning btn-small' %>
       </div>
     </li>
  <form class="navbar-form navbar-left" role="search">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
      </form>

  <div class='btn-group nav_item navbar-right'>
      <%= link_to '#', class: 'btn btn-warning btn-small dropdown-toggle', 'data-toggle' => 'dropdown' do %>
      Choose a Region <span class='caret'></span>
      <% end %>
        <ul class='dropdown-menu'>
          <% Region.all.each do |region| %>
            <li>
              <%= link_to region.name, region_path(region)%>
            </li>
          <% end %>
        </ul>
    </div>

  </ul>
</div>