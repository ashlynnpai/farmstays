.container.user-profile-public
  %h3= @user.name
  %h3 Reviews
  - @user.reviews.each do |review|
    .well
      %h4= link_to review.farm.name, farm_path(review.farm)
      %p= review.updated_at
      %p=  review.content
  
